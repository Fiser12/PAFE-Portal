# Copia este archivo a seed-remote.env y rellena los secretos.
# seed-remote.env está en .gitignore: NUNCA lo subas al repo.
DATABASE_URL="postgres://usuario:password@host/db?sslmode=require"
PAYLOAD_SECRET="el-payload-secret-del-entorno"
